/**
 * Obsidian Craft app shell: single-page editorial portfolio with dark-first contrast,
 * content-driven sections, and no runtime dependency on any platform service.
 */
import Home from "@/pages/Home";

export default function App() {
  return <Home />;
}
